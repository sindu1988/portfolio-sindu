# Sindu Tamilarasan — Portfolio

This is a small React + Vite portfolio template using Tailwind CSS.

Quick start (Windows PowerShell):

```powershell
cd d:\Photfolio
npm install
npm run dev
```

Notes:
- The profile photo is referenced from `Image/sindu.JPG` (already in the workspace).
- Place `sindu_resume2025.docx` at the project root to enable the resume download link.
